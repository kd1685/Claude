# ─── Ascent Terminal — Python dependencies ──────────────────────────────────────
#
# QUICK START (web terminal only):
#   pip install -r requirements.txt
#
# Then run:
#   uvicorn app:app --reload --port 8000
#
# ─── Web server (required) ────────────────────────────────────────────────────
fastapi==0.111.0          # REST API framework
uvicorn[standard]==0.29.0 # ASGI server (the [standard] extra adds websocket + reload support)
python-multipart==0.0.9   # needed by FastAPI for form/body parsing

# ─── HTTP client (required) ───────────────────────────────────────────────────
requests==2.32.2          # used by exchanges.py, market_intel.py, macro_data.py, alert webhooks

# ─── Personal-data encryption (GDPR) ─────────────────────────────────────────
# Encrypts stored customer emails at rest (set DATA_KEY in .env — see
# privacy.py docstring for the one-line key generator).
cryptography>=42.0.0

# ─── Liquidation feed (recommended) ──────────────────────────────────────────
# Streams Binance USDT-perp force orders for the live liquidation tiles +
# cascade detection. Pure-python, tiny. Disable the feed with LIQ_FEED=false.
websocket-client==1.8.0

# ─── Exchange data (optional but strongly recommended) ────────────────────────
# Without ccxt the platform falls back to MEXC native API only.
# With ccxt every exchange in the broker picker works (Binance, Bybit, OKX …).
ccxt==4.3.11              # 100+ exchange connectors; comment out to run MEXC-only

# ─── Scalper bot — bots/scalper_bot.py (optional) ────────────────────────────
# Only needed if you run the PyQt5 desktop scalper. Not required for the web terminal.
# PyQt5==5.15.10
# websocket-client==1.8.0

# ─── Forex / Stocks / Indices data — platform/forex_data.py (recommended) ────
# Enables the Forex, Stocks, Indices and Metals tabs in the terminal.
# yfinance is free and requires no API key.  Optionally set TWELVEDATA_KEY for
# a more robust data source (free tier: ~800 req/day).
yfinance>=0.2.40          # free multi-asset data (forex, stocks, ETFs, indices)

# ─── Brain / backtests — brain/*.py (optional) ───────────────────────────────
# Only needed for the research scripts (swing_backtest.py, swing_oos.py etc.)
# Not required for the web terminal or bots.
# numpy==1.26.4
# pandas==2.2.2
