fastapi>=0.111
uvicorn[standard]>=0.29
httpx>=0.27          # FastAPI TestClient
requests>=2.31

# Optional — install for extra features:
# ccxt>=4.3            # enables all non-MEXC exchanges (Binance, Bybit, OKX…)
# yfinance>=0.2        # enables forex / indices / stocks / metals charts
# websocket-client>=1.8 # enables the live Binance liquidation feed
# pywebview>=5         # needed to build the desktop .exe (Windows only)
# pyinstaller>=6       # needed to build the desktop .exe (Windows only)
# cryptography>=42     # enables email encryption at rest (DATA_KEY)
# pytest>=8            # run the test suite: python -m pytest tests -q
