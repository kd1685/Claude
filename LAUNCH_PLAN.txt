════════════════════════════════════════════════════════════════════
 ASCENT TERMINAL — LAUNCH PLAN (do these in order, top to bottom)
════════════════════════════════════════════════════════════════════

Everything below is YOUR action. Each box is a checkbox.
Estimated total time: 45–90 minutes (excluding Stripe which is tomorrow).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PHASE 1 — DEPLOY THE CODE (today)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] 1. Run UPDATE.bat from inside this folder.
       - Rsyncs code to VPS, skips .env / keys.json / data/
       - Takes ~2 minutes on a normal connection

[ ] 2. SSH to VPS and install new dependencies:
       ssh user@<vps-ip>
       cd /opt/ascent && source venv/bin/activate
       pip install -r requirements.txt

[ ] 3. Restart the platform service:
       sudo systemctl restart ascent
       sudo systemctl status  ascent   ← must say "active (running)"

[ ] 4. If you run the scalper as a separate service:
       sudo systemctl restart ascent-scalper

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PHASE 2 — VERIFY (today, 10 min)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] 5. Open https://ascentterminal.com — no JS errors in console
[ ] 6. /download page — download button works, file actually lands
[ ] 7. /pricing page — tiers display correctly
[ ] 8. Log in as a test user — dashboard loads without errors

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PHASE 3 — STRIPE LIVE (tomorrow)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] 9.  Stripe dashboard → Developers → API keys → copy sk_live_…
[ ] 10. Edit VPS .env: replace sk_test_… with sk_live_…
[ ] 11. Update STRIPE_WEBHOOK_SECRET with live signing secret
[ ] 12. sudo systemctl restart ascent
[ ] 13. Do one real £1 test purchase end-to-end

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 PHASE 4 — BOTS & EDGE LAB (today or tomorrow)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] 14. Run edge lab dry-run on VPS:
        cd /opt/ascent/brain && python edge_lab.py --dry-run

[ ] 15. (Optional) full backtest on Windows:
        Double-click brain/RUN_EDGE_LAB.bat
        Confirm Sharpe > 1 in brain/edge_results.csv

[ ] 16. Monitor scalper heartbeat:
        curl http://localhost:8001/health
        Expected: {"status": "ok"}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ALL BOXES TICKED = YOU ARE LIVE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
