# ✅ WHAT TO DO NOW — owner runbook (post-upgrade)

Everything in the platform is built and tested. These are YOUR steps, in order.
Most take minutes. The only slow one is #8 (the scalper backtest you asked about).

────────────────────────────────────────────────────────
TODAY (30–45 min)
────────────────────────────────────────────────────────

1. DEPLOY THE UPDATE
   ▶ Windows: open a terminal in this folder, run:  UPDATE.bat
   ▶ It rsyncs the code to the VPS, skips .env / keys.json / data/
   ▶ Then SSH in and:  pip install -r requirements.txt
                       sudo systemctl restart ascent

2. VERIFY THE WEBSITE
   ▶ https://ascentterminal.com  — no JS errors
   ▶ /download  — button works, file downloads
   ▶ /pricing   — tiers correct
   ▶ Log in as test user — dashboard loads

3. SMOKE-TEST THE SCALPER
   ▶ On VPS:  cd /opt/ascent/brain && python edge_lab.py --dry-run
   ▶ Should exit 0 with a signal summary, no tracebacks

4. (OPTIONAL) FULL BACKTEST — only if you changed strategy params
   ▶ Windows: double-click  brain/RUN_EDGE_LAB.bat
   ▶ Results in brain/edge_results.csv  (Sharpe should be > 1)

────────────────────────────────────────────────────────
TOMORROW (15 min)
────────────────────────────────────────────────────────

5. SWITCH STRIPE TO LIVE
   ▶ Stripe dashboard → Developers → API keys → copy sk_live_…
   ▶ Edit VPS .env:  STRIPE_SECRET_KEY=sk_live_…
   ▶ Also update:    STRIPE_WEBHOOK_SECRET=whsec_…  (live endpoint)
   ▶ sudo systemctl restart ascent
   ▶ Do one real £1 test purchase

────────────────────────────────────────────────────────
NOT YOUR JOB (already done)
────────────────────────────────────────────────────────

  ✓ Scalper bot rewritten (ATR sizing, fees, Redis heartbeat, Prometheus)
  ✓ Edge lab upgraded (parallel walk-forward, regime filter, HTML report)
  ✓ MEXC trend bot hardened (Kelly sizing, drawdown breaker, reconciliation)
  ✓ Website download page wired to real binary
  ✓ UPDATE.bat and organise.bat written
