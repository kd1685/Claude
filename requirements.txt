# Core backend
fastapi>=0.110
uvicorn[standard]>=0.27

# --- Optional: real device automation + OCR rankings scanner ---
# These are only needed when CONTROL_BACKEND=adb (a real Android device or
# emulator running Rise of Kingdoms, reachable over ADB). The app runs fully
# without them using the built-in mock adapter.
#
#   pip install -r requirements-adb.txt
#
# pillow>=10.0          # screenshot handling
# opencv-python>=4.8    # cropping / preprocessing the rankings rows
# pytesseract>=0.3.10   # OCR (also requires the `tesseract` binary installed)
# numpy>=1.26
